# Copy to .env. Use the SAME MongoDB the Node server uses.
MONGODB_URI=mongodb://localhost:27017/asalpasal

# Only needed if MONGODB_URI has no /dbname path:
# MONGO_DB=asalpasal

MODEL_PATH=model.pkl
PORT=8000
